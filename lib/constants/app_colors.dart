import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.black;
  static const Color secondary = Colors.deepPurple;
  static const Color background = Colors.white;
  static const Color textPrimary = Colors.black;
  static final Color textSecondary = Colors.grey[600]!;
  static final Color cardBackground = Colors.white;
  static final Color divider = Colors.grey[300]!;
  
  // Gradient colors
  static const Color streakStart = Color(0xFFFF6B6B);
  static const Color streakEnd = Color(0xFFFF8E53);
  static const Color completedStart = Color(0xFF56CCF2);
  static const Color completedEnd = Color(0xFF2F80ED);
  
  // Status colors
  static const Color success = Colors.green;
  static const Color warning = Colors.orange;
  static const Color error = Colors.red;
  static const Color info = Colors.blue;
}
